# Configuración para entorno Docker local
cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# URLs de microservicios en la red de Docker
clients.auth.base-url=${MSVC_AUTH_URL:http://msvc-auth:8001}
clients.usuario.base-url=${MSVC_USUARIO_URL:http://msvc-usuario:8095}
clients.productos.base-url=${MSVC_PRODUCTOS_URL:http://msvc-productos:8003}
clients.carrito.base-url=${MSVC_CARRITO_URL:http://msvc-carrito:8008}
clients.pedido.base-url=${MSVC_PEDIDO_URL:http://msvc-pedido:8085}
clients.inventario.base-url=${MSVC_INVENTARIO_URL:http://msvc-inventario:8004}
clients.referidos.base-url=${MSVC_REFERIDOS_URL:http://msvc-referidos:8005}
clients.resenia.base-url=${MSVC_RESENIA_URL:http://msvc-resenia:8010}
clients.pagos.base-url=${MSVC_PAGOS_URL:http://msvc-pagos:8011}
clients.notificaciones.base-url=${MSVC_NOTIFICACIONES_URL:http://msvc-notificaciones:8006}
clients.eventos.base-url=${MSVC_EVENTOS_URL:http://msvc-eventos:8092}
clients.contenido.base-url=${MSVC_CONTENIDO_URL:http://msvc-contenido:8093}
clients.promociones.base-url=${MSVC_PROMOCIONES_URL:http://msvc-promociones:8091}


